#!/usr/bin/env bash
# Executable: /bin/detect <platform[AR]> <plan[E]>, Working Dir: <app[AR]>
set -eo pipefail

platform_dir="$1"
plan="$2"

echo "plan: ${plan}"
#ls -l ${plan}
echo "platform_dir: ${platform_dir}"

#Always apply this watermark build pack
exit 0
